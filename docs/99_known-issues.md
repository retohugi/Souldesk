# currently known issues

## portal configuration
- adding a new project provides the option to "test connection" which is not working.
- adding a new project does not reload the dropdown selection on the cards, thus the new project cannot be selected. if a new project is added, the dropdowns on hte insights card should be refreshed.

